import { AppFrame } from './components/AppFrame'
import './App.css'

function App() {
  return (
    <AppFrame>
      {/* App Canvas: future pages (home, visit note, patient profile, booking) will render here */}
    </AppFrame>
  )
}

export default App
